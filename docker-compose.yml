version: "3.8"
services:
  server:
    container_name: helpfulai
    build:
      context: "."
      dockerfile: Dockerfile
    volumes:
      - ~/.aws:/root/.aws:ro
      - ./src:/wd
    command: ["python", "src/main.py"]